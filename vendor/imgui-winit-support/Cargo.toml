[package]
name = "imgui-winit-support"
version = "0.13.0"
edition = "2021"
description = "winit support code for the imgui crate (vendored, Android-compatible)"
license = "MIT OR Apache-2.0"

[dependencies]
imgui = "0.12"
winit = { version = "0.30", default-features = false }
